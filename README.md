# rc_word_game

build:
```
    cd PLAYER
    make
    cd ../SERVER
    make
```

server timeout:
```
    tempo timeout predefinido - server.h (linha 5)
    configuração timeout da socket - server.c (linha 169)
```

